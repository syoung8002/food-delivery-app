apiVersion: v1
kind: Service
metadata:
  name: customer-management
  labels:
    app: customer-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: customer-management